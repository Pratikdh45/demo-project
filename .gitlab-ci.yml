stages:
  - build
  - test
  - docker
  - docker_push

variables:
  MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version"
  MAVEN_OPTS: "-Dmaven.repo.local=${CI_PROJECT_DIR}/.m2/repository"
  DOCKER_IMAGE_PREFIX: "172.30.38.4:8083/repository/docker-private/demo-project/quarkus-helloworld"

build:
  stage: build
  image: maven:3.8.4-openjdk-11-slim
  script:
    - mvn $MAVEN_CLI_OPTS clean package -DskipTests
    - mvn package -Dquarkus.container-image.build=true
  artifacts:
    paths:
      - target/*.jar
  tags:
    - master

test:
  stage: test
  image: maven:3.8.4-openjdk-11-slim
  script:
    - mvn $MAVEN_CLI_OPTS test sonar:sonar -Dsonar.host.url=$SONAR_URL -Dsonar.login=$SONAR_LOGIN -Dsonar.password=$SONAR_PASSWORD
  tags:
    - master

docker:
  stage: docker
  image: maven:3.8.4-openjdk-11-slim
  before_script:
    - echo "Preparing to create Docker image..."
  script:
    - mvn $MAVEN_CLI_OPTS compile quarkus:build -Dquarkus.container-image.build=true -Dquarkus.container-image.image=$DOCKER_IMAGE_PREFIX:${CI_COMMIT_REF_SLUG}-${CI_COMMIT_SHORT_SHA}
    - mvn $MAVEN_CLI_OPTS quarkus:build -Dquarkus.container-image.push=true -Dquarkus.container-image.image=$DOCKER_IMAGE_PREFIX:${CI_COMMIT_REF_SLUG}-${CI_COMMIT_SHORT_SHA}
  tags:
    - master
    
docker_push:
  stage: docker_push
  image:
    name: docker:19.03.14
  variables:
    DOCKER_HOST: tcp://dind.gitlab-runner:2375
  services:
  - name: docker:19.03.14-dind
    entrypoint: ["env", "-u", "DOCKER_HOST"]
    command: ["dockerd-entrypoint.sh"]
  before_script:
    - mkdir -p $HOME/.docker
    - echo $DOCKER_AUTH_CONFIG > $HOME/.docker/config.json
    - echo "Preparing to push Docker image..."
  script:
    - docker images
    - docker load --input target/*.jar
    - docker push $DOCKER_IMAGE_PREFIX:${CI_COMMIT_REF_SLUG}-${CI_COMMIT_SHORT_SHA}
  tags:
    - master
